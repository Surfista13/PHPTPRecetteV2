{% extends 'base.html.twig' %}

{% block title %}Liste de mes recettes{% endblock %}

{% block body %}
    <nav>
        <ul class="ul_nav">
            <li><a href="{{ path('app_accueil') }}">Accueil</a></li>
            <li><a href="{{ path('app_liste') }}">Liste</a></li>
            <li>Détails</li>
        </ul>
    </nav>
    <main>
        <div class="detailsRecette">
            <h1>{{ details.nom }}</h1>
            <img src="{{ asset('images/'~details.image)}}">
            <p>{{ details.description }}</p>
        </div>
    </main>
{% endblock %}
